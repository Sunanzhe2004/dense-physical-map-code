# Data Notes

This repository does not ship the full dataset.

- Put tiny public examples in `data/samples/`.
- Put real local-only data in `data/raw/`, `data/intermediate/`, or `data/private/`.
- Keep split definitions in `data/splits/`.
